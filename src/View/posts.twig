{% extends "base.twig" %}

{% block title %}News{% endblock %}

{% block content %}
    <h1>Posts</h1>
    {% for post in posts %}
        <h3>{{ post.getTitle() }}</h3>
        <p>{{ post.getContent() }}</p>
    {% endfor %}
{% endblock %}
